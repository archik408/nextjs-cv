import { TimeMachineContent } from '@/components/time-machine-content';

export const metadata = {
  title: 'Time Machine • arturbasak.dev',
  description:
    'Explore the evolution of arturbasak.dev over the years. See how the website has changed and improved through different versions.',
};

export default function TimeMachinePage() {
  return <TimeMachineContent />;
}
